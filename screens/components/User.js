export default class User {
    constructor(username, email, uid, permitType, vehicleType) {
        this.username = username;
        this.email = email;
        this.uid = uid;
        this.permitType = permitType;
        this.vehicleType = vehicleType;
    }
}